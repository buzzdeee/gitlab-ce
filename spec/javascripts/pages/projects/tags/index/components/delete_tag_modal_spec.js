// import deleteTagModal from '~/pages/projects/tags/components/delete_tag_modal.vue';

describe('delete_tag_modal.vue', () => {
  describe('methods', () => {
    describe('canSubmit', () => {
      it('checks confirmation', () => {
        fail('TODO');
      });
    });

    describe('onSubmit', () => {
      it('redirects if deleting was successful', () => {
        fail('TODO');
      });

      it('shows error message if deleting failed', () => {
        fail('TODO');
      });
    });
  });
});
