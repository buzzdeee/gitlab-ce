.scrolling-tabs-container.inner-page-scroll-tabs.is-smaller
  .fade-left= icon('angle-left')
  .fade-right= icon('angle-right')
  %ul.nav-links.search-filter.scrolling-tabs
    - if @project
      - if project_search_tabs?(:blobs)
        %li{ class: active_when(@scope == 'blobs') }
          = link_to search_filter_path(scope: 'blobs') do
            Code
            %span.badge
              = @search_results.count(:blobs)
      - if project_search_tabs?(:issues)
        %li{ class: active_when(@scope == 'issues') }
          = link_to search_filter_path(scope: 'issues') do
            Issues
            %span.badge
              = @search_results.count(:issues)
      - if project_search_tabs?(:merge_requests)
        %li{ class: active_when(@scope == 'merge_requests') }
          = link_to search_filter_path(scope: 'merge_requests') do
            Merge requests
            %span.badge
              = @search_results.count(:merge_requests)
      - if project_search_tabs?(:milestones)
        %li{ class: active_when(@scope == 'milestones') }
          = link_to search_filter_path(scope: 'milestones') do
            Milestones
            %span.badge
              = @search_results.count(:milestones)
      - if project_search_tabs?(:notes)
        %li{ class: active_when(@scope == 'notes') }
          = link_to search_filter_path(scope: 'notes') do
            Comments
            %span.badge
              = @search_results.count(:notes)
      - if project_search_tabs?(:wiki)
        %li{ class: active_when(@scope == 'wiki_blobs') }
          = link_to search_filter_path(scope: 'wiki_blobs') do
            Wiki
            %span.badge
              = @search_results.count(:wiki_blobs)
      - if project_search_tabs?(:commits)
        %li{ class: active_when(@scope == 'commits') }
          = link_to search_filter_path(scope: 'commits') do
            Commits
            %span.badge
              = @search_results.count(:commits)

    - elsif @show_snippets
      %li{ class: active_when(@scope == 'snippet_blobs') }
        = link_to search_filter_path(scope: 'snippet_blobs', snippets: true, group_id: nil, project_id: nil) do
          Snippet Contents
          %span.badge
            = @search_results.count(:snippet_blobs)
      %li{ class: active_when(@scope == 'snippet_titles') }
        = link_to search_filter_path(scope: 'snippet_titles', snippets: true, group_id: nil, project_id: nil) do
          Titles and Filenames
          %span.badge
            = @search_results.count(:snippet_titles)

    - else
      %li{ class: active_when(@scope == 'projects') }
        = link_to search_filter_path(scope: 'projects') do
          Projects
          %span.badge
            = limited_count(@search_results, :projects, 1000)
      %li{ class: active_when(@scope == 'issues') }
        = link_to search_filter_path(scope: 'issues') do
          Issues
          %span.badge
            = limited_count(@search_results, :issues, 1000)
      %li{ class: active_when(@scope == 'merge_requests') }
        = link_to search_filter_path(scope: 'merge_requests') do
          Merge requests
          %span.badge
            = limited_count(@search_results, :merge_requests, 1000)
      %li{ class: active_when(@scope == 'milestones') }
        = link_to search_filter_path(scope: 'milestones') do
          Milestones
          %span.badge
            = limited_count(@search_results, :milestones, 1000)
